# Base URL of the Railway backend (Express + Postgres). Server-side only — the
# Next route handlers under app/api/* proxy to it, so it is never exposed to the
# browser. Set this in Vercel project settings and in a local .env.local.
RAILWAY_API_URL=https://your-app.up.railway.app
